import { Routes } from '@angular/router';
import { DayOneComponent } from './adventOfCode/day-one/day-one.component';

export const routes: Routes = [
  {path: 'advent/day-one', component: DayOneComponent},
];
